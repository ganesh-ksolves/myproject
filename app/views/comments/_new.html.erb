<h1>New Comment</h1>

<%= form_with(model: Comment.new, url: article_comments_path(@article)) do |form| %>
  <%= form.label :content %>
  <%= form.text_area :content %>

  <%= form.submit %>
<% end %>
